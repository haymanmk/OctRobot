[pytest]
# Disable globally-installed ROS pytest plugins (launch_testing, launch_ros) that
# are unrelated to this project and break collection under mismatched Python versions.
# `-p no:<plugin>` is a no-op when the plugin is absent (e.g. CI without ROS).
addopts = -p no:launch_testing -p no:launch_ros
